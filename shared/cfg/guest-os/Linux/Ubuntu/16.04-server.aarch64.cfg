- 16.04-server.aarch64:
    image_name += -16.04-lts-aarch64
    vm_arch_name = aarch64
    os_variant = ubuntuxenial
    unattended_install.import, svirt_install:
        unattended_file = unattended/
